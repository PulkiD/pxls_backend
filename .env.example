# Server Configuration
NODE_ENV=development
PORT=3000 # Or any port you prefer for the backend

# CORS Configuration
# For development, you might use a wildcard or your local frontend URL.
# For production, specify the exact domain of your frontend.
CORS_ORIGIN=http://localhost:5173 # Example: if your frontend runs on port 3000

# Logging Configuration
LOG_LEVEL=info # e.g., error, warn, info, http, verbose, debug, silly

# API Version (Optional, but good practice)
API_PREFIX=/api

# Add any other external service API keys or configurations your BFF might need in the future
# EXAMPLE_EXTERNAL_SERVICE_URL=
# EXAMPLE_EXTERNAL_SERVICE_API_KEY=

USE_MOCK_DATA=true